'use client'

import React, {Component} from "react";


const LoginPage = () => {
  return <div id="leaves">page<button onClick={() => console.log('Click')}>Add to Cart</button></div>;
};

export default LoginPage;
